colang_version: "2.x"

models:
  - type: main
    engine: google_vertexai
    model: gemini-2.5-flash-lite

rails:
  config:
    sensitive_data_detection:
      input:
        entities:
          - PERSON
          - PHONE_NUMBER
          - EMAIL_ADDRESS
          - US_SSN
      